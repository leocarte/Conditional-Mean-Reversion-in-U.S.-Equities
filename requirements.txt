# MLfinance — pinned dependencies for reproducible builds.
# Matches pyproject.toml [project.dependencies] with exact `==` pins.
# Refresh by re-running `pip install -r requirements.txt` after edits.
torch==2.5.1
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
xgboost==3.0.3
lightgbm==4.5.0
statsmodels==0.14.6  # >=0.14.5: 0.14.4 imports scipy._lazywhere, removed in scipy 1.15
hydra-core==1.3.2
pyarrow==18.1.0
pyyaml==6.0.2
tqdm==4.67.1
matplotlib==3.9.3
